import images from "./images";
import icons from "./icons";
import { COLORS, SIZES, FONTS } from './themes';
import GOOGLE_API_KEY from './maps';

export {images, icons, COLORS, SIZES, FONTS, GOOGLE_API_KEY};

// We import here everything and use export it from index.js so that it is usable everywhere